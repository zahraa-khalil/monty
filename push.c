#include "monty.h"
/*
#define STACK_SIZE 100

typedef struct
{
	int stack[STACK_SIZE];
	int top;
} Stack;

void init(Stack *s)
{
	s->top = -1;
}

int push(Stack *s, int value)
{
	if (atoi(value))
	{

		s->stack[++s->top] = value;
		return (EXIT_SUCCESS);
	}
	else
	{
		printf("L<%d>: usage: push integer", value);
		return (EXIT_FAILURE);
	}


}
*/